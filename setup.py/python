from setuptools import setup, find_packages

setup(
    name="monitoramento_sensores",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Adicione aqui as dependências do projeto
    ],
)
